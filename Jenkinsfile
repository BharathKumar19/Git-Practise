pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build the pipeline'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy the pipeline'
            }
        }
        stage('Test') {
            steps {
                echo 'Test the pipeline'
            }
        }
    }
}
